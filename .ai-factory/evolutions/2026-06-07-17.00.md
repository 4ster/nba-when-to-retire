# Evolution: 2026-06-07 17:00

## Intelligence Summary
- Patches analyzed: 0 new (cursor at 2026-06-07-13.15.md; both existing patches already
  distilled in the 2026-06-07-13.30 run). Cursor NOT advanced.
- Source of improvements: codebase conventions (Step 5.3) from the dataset-refresh +
  methodology-modal feature (committed and verified this session).
- Tech stack: Python (pandas) build-time pipeline + single-file d3 HTML.

## Improvements Applied

### aif-architecture → skill-context (1 rule strengthened)
- Per-dataset adapter rule extended: loader input may be a directory; adapters may JOIN
  multiple source CSVs; prefer native id over factorize; source-specific traded markers
  (TOT vs 2TM/3TM) ← driven by codebase (`load_sumitrodatta`)
  **File:** `.ai-factory/skill-context/aif-architecture/SKILL.md`

### aif-implement → skill-context (1 new + 1 strengthened)
- New: derive data-recency label + season_min/max from data into `meta`, never hardcode
  in Taskfile/CLI ← driven by codebase (`build.season_label`) + RULES data-date requirement
- Strengthened: external-data robustness now covers wrong-TYPE values — float `NaN` is
  truthy so `(pos or "").strip()` raises; don't assume `player_id` is int (native string ids)
  ← driven by codebase (`position_group`, `notable._native_id`)
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`

### aif-verify → skill-context (1 rule strengthened)
- Headless render-verify extended to modal/overlay dialogs: open via trigger, assert focus
  trap, Esc + backdrop close, focus return, role=dialog/aria-modal ← driven by codebase
  (`tools/verify_dom.mjs`)
  **File:** `.ai-factory/skill-context/aif-verify/SKILL.md`

## Patterns Identified
- external-data type-safety: 3rd facet (NaN-truthy `or` trap, native string id) — recurring
- build-time → runtime metadata derivation (data labels): 1 occurrence
- headless a11y verification (dialog focus management): 1 occurrence

## Note
The two real-data crashes this session (NaN position, string player_id in notable) were fixed
inline via /aif-implement, so no patch files exist for them. Their lessons are captured here as
skill-context. If a patch trail is desired, retro-document them under .ai-factory/patches/.
