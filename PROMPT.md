Собираем single-file HTML scrollytelling-дашборд «Игроки NBA: когда пора на пенсию?».
spec.md в корне — источник истины, прочитай его целиком и следуй ему. Перед кодом
зафиксируй ответы на §7 (предлагаю дефолты: value = BPM; эпохи ≤1989 / 1990–2009 / 2010+;
порог включения сезона ≥ 1000 минут).

Работаем в две фазы, тесты раньше реализации:

Фаза 1 — пайплайн данных (Python):
- скачать датасет Kaggle из §2, посчитать производные агрегаты (aging_curve дельта-методом,
  survival, skill_decline, notable) на скоростных метриках per-36;
- сначала тесты: корректность дельта-метода, нормализация per-36, обработка survivorship bias,
  схема и бюджет размера (<300 КБ);
- выход: один слим-JSON по контракту §2.

Фаза 2 — single-file HTML:
- встроить JSON как <script type="application/json">, реализовать сцены US-1…US-7 и интеракции §4;
- гейт приёмки — чек-листы §0.4/§6 (минимум не-данных, прямые подписи, ≤2 шрифтов, theme-aware
  цвет, один JS-блок, node --check, версии CDN зафиксированы);
- соблюдать non-goals §5: это скроллителлинг, не BI-сетка.

Не начинай Фазу 2, пока Фаза 1 не прошла тесты.
