# DESIGN — enforceable-решения арт-дирекции

Извлечение из `BRIEF.md` в формате, проверяемом `/aif-verify`. После принятия пункты
переносятся в `RULES.md` (правила) и `spec.md` (токены/сцены).

**Юрисдикция:** только презентационный слой (CSS, SVG/Canvas-отрисовка, тексты UI,
хореография). Контракт данных (дельта-метод, per-36, слим-JSON < 300 КБ), схема агрегатов
и офлайн-пайплайн — вне юрисдикции этого документа и не меняются.

---

## 1. Токены

### 1.1 Цвет (hex; всё через CSS-переменные)

| Токен | Тёмная (default) | Светлая | Смысл |
|---|---|---|---|
| `--bg` | `#10131A` | `#FAF7F1` | арена / бумага программки |
| `--surface` | `#181C25` | `#FFFFFF` | карточки шагов, панель US-7 |
| `--fg` | `#EDE9E1` | `#1C1B18` | тёплый белый / типографская краска |
| `--muted` | `#9A968D` | `#6E6A61` | вторичный текст, погасшие линии |
| `--hair` | `#262B33` | `#E7E2D8` | разделители |
| `--accent` | `#FF8A3D` | `#D9540D` | оранжевый мяча — только смысловые точки |
| `--label` | `#F2EFE9` | `#1A1A1A` | метки данных (theme-aware) |
| `--c-guard` | `#5E9EFF` | `#2563EB` | защитники |
| `--c-wing` | `#E8B339` | `#B97E04` | винги |
| `--c-big` | `#E76F51` | `#B3402A` | большие |
| `--c-era-pre` | `#8B9099` | `#7A7F88` | ранняя эпоха (ч/б хроника) |
| `--c-era-mid` | `#B3A0E6` | `#6D5BC7` | средняя эпоха (плёнка) |
| `--c-era-modern` | `#35C4B5` | `#0E8A7D` | современная эпоха (LED) |
| `--ghost` | `#6E7480` | `#A8A49B` | средняя-«призрак» (US-5, US-7) |
| `--band` | `rgba(255,255,255,.08)` | `rgba(28,27,24,.08)` | доверительный коридор |
| `--jersey` | `rgba(237,233,225,.07)` | `rgba(28,27,24,.06)` | джерси-номер фоном |

### 1.2 Шрифты

| Токен | Значение |
|---|---|
| `--font-display` | `"Oswald", "Arial Narrow", "Segoe UI", sans-serif` |
| `--font-text` | `-apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif` |

Oswald: woff2, сабсет latin + cyrillic, веса **500 и 600**, встроен base64 в `@font-face`
внутри `index.html`. Суммарный бюджет встроенных шрифтов ≤ 90 КБ (base64).
`font-display: swap` не нужен (шрифт инлайн); `font-variant-numeric: tabular-nums`
на всех числовых элементах.

### 1.3 Типографическая шкала

| Токен | Значение | Применение | Семейство/вес |
|---|---|---|---|
| `--fs-giant` | `clamp(6rem, 18vw, 11rem)` | джерси-номер | Oswald 600 |
| `--fs-h1` | `clamp(2.5rem, 6vw, 4.25rem)` | заголовок интро | Oswald 600 |
| `--fs-h2` | `clamp(1.6rem, 3vw, 2.1rem)` | заголовки сцен | Oswald 500 |
| `--fs-kicker` | `0.75rem`, caps, ls `0.08em` | «СЦЕНА N» | Oswald 500 |
| `--fs-lede` | `1.15rem` | лид интро | системный 400 |
| `--fs-body` | `1.0625rem` (17px) | текст шагов | системный 400 |
| `--fs-label` | `0.8125rem` (13px) | метки данных, тултипы | системный 600 |

### 1.4 Отступы и геометрия

База 4px: `--s-1:4px; --s-2:8px; --s-3:12px; --s-4:16px; --s-6:24px; --s-8:32px;
--s-12:48px; --s-16:64px`. Колонка текста шагов ≤ `26rem`; зазор между шагами ≥ `55vh`
(сцена успевает отыграть морф). Радиус поверхностей `10px`, один на всё.
Толщины штрихов: линия-герой `3px`; кривые амплуа/эпох `2.5px`; призрак US-5/US-7 —
полоса `12px` цвета `--ghost` с opacity `.35`; пунктир ранней эпохи `4 4`,
толщина `1.75px`.

### 1.5 Тайминги

| Токен | Значение | Применение |
|---|---|---|
| `--t-fast` | `200ms` | hover, тултипы, кнопки |
| `--t-step` | `320ms` | вход текстового шага (fade + 12px) |
| `--t-scene` | `600ms` | морф графики между сценами |
| `--t-fall` | `900ms` | осыпание точек US-6 (stagger ≤ 30ms/точка) |
| `--t-theme` | `300ms` | переключение темы |
| `--ease` | `cubic-bezier(0.22, 1, 0.36, 1)` | все переходы |

При `prefers-reduced-motion: reduce` все `--t-*`, кроме `--t-theme`, → `0ms`
(допустим opacity-кроссфейд ≤ 150 мс).

---

## 2. Правила (MUST / MUST NOT)

### Типографика
- MUST: ровно 2 семейства — Oswald (display) и системный гротеск (текст/данные); роли по §1.3.
- MUST: Oswald встроен base64-woff2 (сабсет latin+cyrillic, веса 500/600), бюджет ≤ 90 КБ.
- MUST NOT: рантайм-загрузка шрифтов (Google Fonts, любой CDN) — ломает офлайн-открытие.
- MUST: размеры шрифта — только из токенов `--fs-*`; `tabular-nums` на числах.
- MUST NOT: Oswald в основном тексте, метках данных и UI-контролах.
- MUST NOT: третье семейство, включая моноширинное (`--mono` из v1 удалить;
  выравнивание чисел решает `tabular-nums`).

### Цвет
- MUST: все цвета — через токены §1.1; JS читает их `getComputedStyle`.
- MUST NOT: hex-литералы в JS-коде отрисовки.
- MUST: стартовая тема — тёмная (`<html data-theme="dark">`); светлая полнофункциональна.
- MUST: `--accent` появляется не более чем в одном смысловом элементе на сцену
  (пик US-1, флажок медианы US-6, активные контролы US-7, заливка хвоста US-4).
- MUST: единственный градиент в проекте — выцветание штриха кривой после пика
  (кодирует спад, привязан к данным).
- MUST NOT: градиенты фонов/карточек, свечения, тени глубже 4px (повтор RULES, остаётся в силе).

### Сцены (анти-generic)
- MUST: на секции каждой сцены атрибут `data-signature` со значением из таблицы:
  `us1-mountain`, `us2-split`, `us3-board`, `us4-film`, `us5-spotlight`, `us6-cliff`,
  `us7-clipboard`; значения уникальны, приёмы реализованы по `BRIEF.md §4`.
- MUST: один sticky-канвас на все сцены; смена сцен — update/морф существующих элементов.
- MUST NOT: пересоздание канваса или полный перерендер DOM при смене сцены.
- MUST: US-3 — единственная сетка small multiples; US-6 — единственное распределение;
  US-7 — единственная сцена с контролами.
- MUST: джерси-номер US-1 — значение возраста пика из данных, цвет `--jersey`,
  позади графики, не пересекает аннотации и метки.
- MUST: эпохи US-4 различаются и цветом, и фактурой штриха (пунктир / толщина) —
  читаемо при ч/б печати и дальтонизме.

### Хореография
- MUST: длительности и easing — только из токенов §1.5.
- MUST: ровно два расширенных перехода («расщепление» US-1→2, «осыпание» US-5→6);
  остальные морфы — стандартные `--t-scene`.
- MUST: при `prefers-reduced-motion` морфы заменяются мгновенной сменой состояния;
  все выводы доступны без анимации.
- MUST NOT: параллакс, зацикленные анимации, движение, не кодирующее смену состояния данных.

### Редакторский голос
- MUST: заголовок сцены — утверждение ≤ 7 слов, содержит число или глагол.
- MUST NOT: номинативные заголовки-этикетки («Средняя кривая», «Эпохи», «Динамика…»).
- MUST: аннотация ≤ 12 слов, одна мысль, настоящее время.
- MUST: каждое число в заголовках/аннотациях совпадает с агрегатами слим-JSON;
  проверяется `console.assert` по ключевым числам (пик, дивергенция, медиана).

### Сборка (дополнение к RULES §«Сборка»)
- MUST: суммарный вклад дизайна (встроенные шрифты + CSS) ≤ 150 КБ; бюджет JSON < 300 КБ
  не затрагивается.
- MUST: библиотеки (d3, scrollama) вендорятся инлайн в `index.html` минифицированными;
  версия и источник — в комментарии над блоком. Бюджет вендора ≤ 350 КБ
  (полный `d3.min` ~280 КБ допустим; микросборка нужных модулей — предпочтительна).
- MUST NOT: внешние `<script src>` / `<link href>` в финальном файле — строгий офлайн:
  открывается двойным кликом без сети.

---

## 3. Соответствие spec / RULES

| Решение DESIGN | Норма | Статус |
|---|---|---|
| Шрифты base64 инлайн, без рантайм-фетчей | RULES «один self-contained HTML», «MUST NOT рантайм-загрузка» | ✓ жёстче нормы: работает офлайн |
| d3/scrollama вендорятся инлайн вместо CDN | RULES «CDN с зафиксированными версиями» | требует правки RULES (п. 5 ниже): строгий офлайн жёстче CDN-нормы |
| Один sticky-канвас, морфы сцен | spec §0.2 «скроллителлинг, не BI»; KPI-сетка не вводится | ✓ |
| Все приёмы US-1…7 работают на готовых агрегатах (`aging_curve`, `skill_decline`, `survival`, `notable`) | spec §2 контракт данных, дельта-метод, слим-JSON | ✓ данные не меняются |
| Оси/сетки/рамки не возвращаются; прямые подписи, значения на ключевых точках | скил §5.1, RULES «Дата-виз» | ✓ |
| Джерси-номер = возраст пика из данных (укрупнённая прямая подпись), opacity ≈ .07 | скил §5.1 «минимум не-данных» | ✓ это данные; контраст ниже `--band`, не мешает чтению |
| Градиент выцветания штриха | RULES «MUST NOT лишний декор — градиенты» | ✓ не декор: кодирует спад; зафиксировать исключение в RULES при принятии |
| Oswald + системный гротеск | RULES «не более 2 шрифтов», скил §8 | ✓ ровно 2 |
| Палитра амплуа/эпох сохраняет семантику v1, оттенки гармонизированы; domain — полный набор | RULES «Цвет и тема», скил §10.4 | ✓ |
| `--label` theme-aware, без обводок | скил §5.3, RULES | ✓ |
| Тёмная тема стартовая | RULES «стартовый `data-theme`» | ✓ норма не фиксирует значение; bump в spec §4 |
| Все морфы отключаемы, выводы читаются без анимации | spec §0.6, RULES «Доступность» | ✓ |
| Заголовки/аннотации на русском | spec §0.5, RULES «Язык» | ✓ |
| Бюджет шрифтов 90 КБ + CSS ≤ 150 КБ | spec §2 бюджет JSON < 300 КБ | ✓ бюджеты раздельны; итоговый файл остаётся «лёгким» |

### Правки, требуемые в RULES/spec при принятии DESIGN

> Статус: внесены в `RULES.md` (секции «Цвет», «Типографика», «Сборка», новая «Арт-дирекция»)
> и `spec.md` (§0.1, §0.4, §3, §4, §6) — 2026-06-07.

1. RULES «Цвет»: добавить исключение «градиент допустим единственно как кодирование спада
   на штрихе кривой».
2. RULES «Типографика»: уточнить «2 шрифта = Oswald (встроенный) + системный гротеск;
   рантайм-загрузка шрифтов запрещена».
3. spec §4: стартовая тема — тёмная.
4. spec §3: заменить рабочие названия сцен на редакторские заголовки (`BRIEF.md §6`)
   после сверки чисел с агрегатами.
5. RULES «Сборка» и spec §0.4/§6: «CDN-ссылки с зафиксированными версиями» →
   «библиотеки вендорятся инлайн (минифицированные, версия в комментарии); внешних
   `<script src>`/`<link>` нет». Строгий офлайн — условие конкурса.
6. RULES «Типографика»: «не более 2 шрифтов» дополнить «третье семейство, включая
   моноширинное, запрещено; числа — `tabular-nums`».
